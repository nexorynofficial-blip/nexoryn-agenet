Developer: please fill this in after wiring up the widget, and send
it back. This is what gets handed to the AI that built the widget,
so keep it concrete — exact names, exact errors, exact behavior.

---

## 1. Where things ended up

- `ProjectForm.tsx` path (if different from
  `admin/src/pages/ProjectForm.tsx`):
- `FloatingAgentWidget.tsx` copied to:
- `nexoryn-logo.png` copied to:
- `VITE_NEXORYN_AGENT_URL` value used:

## 2. The onAutofill handler you wrote

Paste the actual function here, exactly as committed:

```tsx

```

## 3. Anything that didn't map cleanly

For each thing you had to improvise on (not just follow the
example directly), describe what you did and why:

-

## 4. Did you test it end to end?

- [ ] Yes — clicked the widget, submitted a real summary, watched
      fields populate, reviewed them, did NOT click Save Project.
- [ ] Yes — went all the way through and clicked Save Project too.
- [ ] No — only checked it compiles/renders, haven't tried a real
      submission yet.

If yes to either: what worked, what didn't? Screenshots help if
something looks wrong.

## 5. Any errors encountered

Paste exact error messages/stack traces, and where they happened
(browser console, network tab, terminal):

```

```

## 6. Anything you'd flag or push back on

E.g. a naming clash, a place the "don't touch this" constraints from
the request conflicted with something already in the codebase, a
better way you think this should work given how `ProjectForm.tsx` is
actually structured:

-
